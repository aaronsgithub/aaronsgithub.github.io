<ul class="a-listing article-list list">
    <% for (const item of items) { %>
      <li <%= metadataAttrs(item) %> >
          <span class="date listing-date"><%= item.date %></span>
          <a href="<%- item.path %>" class="title listing-name"><%= item.title %></a>
      </li>
    <% } %>
</ul>